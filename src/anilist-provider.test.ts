import { expect, test,  describe, it} from "bun:test";

test("2 + 2", () => {
    expect(2 + 2).toBe(4);
});

describe("Test Anilist NEXT-AUTH Provider", ()=>{
    it('should check', () => {

    });
})